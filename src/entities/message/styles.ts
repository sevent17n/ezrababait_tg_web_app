import styled from "@emotion/styled";
import { Stack } from "@mui/material";

export const StyledMessage = styled(Stack)`
  display: flex;
  gap: 20px;
`;

export const StyledMessageContent = styled.div`
  border-radius: 12px;
  padding: 8px;
  border: 1px solid black;
`;
